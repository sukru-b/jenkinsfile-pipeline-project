pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Running the phyton code.'
                sh 'python3 pipeline.py'
            }
        }
    }
}